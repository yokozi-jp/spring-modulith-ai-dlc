{
  "name": "aidlc-architect-agent",
  "description": "AI-DLC Architect Agent — delegation target for the reverse-engineering (2.1) synthesis step. Use for delegated architecture-analysis tasks.",
  "prompt": "file://aidlc-architect-agent.md",
  "includeMcpJson": true,
  "tools": [
    "fs_read",
    "fs_write",
    "execute_bash",
    "thinking",
    "@context7",
    "@aws-mcp",
    "@aws-pricing",
    "@aws-iac",
    "@aws-serverless"
  ],
  "allowedTools": [
    "fs_read",
    "thinking"
  ],
  "toolsSettings": {
    "execute_bash": {
      "allowedCommands": [
        "bun (run )?[\"']?\\.kiro/tools/[A-Za-z0-9._-]+\\.ts[\"']?( .*)?",
        "date -u( .*)?"
      ],
      "deniedCommands": [
        "([^\\s]*/)?rm( [^\\s]+)* -[A-Za-z]*[rR][A-Za-z]*( .*)?",
        "([^\\s]*/)?rm( [^\\s]+)* --recursive( .*)?",
        "([^\\s]*/)?git( -[^\\s]+( (\"[^\"]*\"|'[^']*'|[^\\s]+))?)* push( .*)?"
      ]
    },
    "fs_write": {
      "allowedPaths": [
        "aidlc/spaces/**"
      ]
    }
  },
  "resources": [
    "file://.kiro/agents/aidlc-architect-agent.md",
    "file://.kiro/knowledge/aidlc-architect-agent/*.md",
    "file://.kiro/knowledge/aidlc-shared/*.md",
    "file://aidlc/spaces/default/memory/**/*.md"
  ],
  "hooks": {
    "preToolUse": [
      {
        "matcher": "execute_bash",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts state-transition-guard aidlc-architect-agent",
        "timeout_ms": 15000
      },
      {
        "matcher": "fs_write",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts review-freeze",
        "timeout_ms": 15000
      },
      {
        "matcher": "execute_bash",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts review-freeze",
        "timeout_ms": 15000
      }
    ],
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts audit-and-sensors",
        "timeout_ms": 120000
      }
    ]
  }
}
