{
  "name": "aidlc-composer-agent",
  "description": "AI-DLC Composer Agent - delegation target for composing a workflow plan (front, report, or in-flight). Proposes the EXECUTE/SKIP grid; after human approval writes the composed scope data.",
  "prompt": "file://aidlc-composer-agent.md",
  "includeMcpJson": true,
  "tools": [
    "fs_read",
    "fs_write",
    "execute_bash",
    "thinking",
    "@context7",
    "@aws-mcp",
    "@aws-pricing",
    "@aws-iac",
    "@aws-serverless"
  ],
  "allowedTools": [
    "fs_read",
    "thinking"
  ],
  "toolsSettings": {
    "execute_bash": {
      "allowedCommands": [
        "bun (run )?[\"']?\\.kiro/tools/[A-Za-z0-9._-]+\\.ts[\"']?( .*)?",
        "date -u( .*)?"
      ],
      "deniedCommands": [
        "([^\\s]*/)?rm( [^\\s]+)* -[A-Za-z]*[rR][A-Za-z]*( .*)?",
        "([^\\s]*/)?rm( [^\\s]+)* --recursive( .*)?",
        "([^\\s]*/)?git( -[^\\s]+( (\"[^\"]*\"|'[^']*'|[^\\s]+))?)* push( .*)?"
      ]
    },
    "fs_write": {
      "allowedPaths": [
        ".kiro/scopes/**",
        ".kiro/tools/data/scope-grid.json"
      ]
    }
  },
  "resources": [
    "file://.kiro/agents/aidlc-composer-agent.md",
    "file://.kiro/scopes/*.md",
    "file://.kiro/knowledge/aidlc-shared/*.md",
    "file://aidlc/spaces/default/memory/**/*.md"
  ],
  "hooks": {
    "preToolUse": [
      {
        "matcher": "execute_bash",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts state-transition-guard aidlc-composer-agent",
        "timeout_ms": 15000
      },
      {
        "matcher": "fs_write",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts review-freeze",
        "timeout_ms": 15000
      },
      {
        "matcher": "execute_bash",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts review-freeze",
        "timeout_ms": 15000
      }
    ],
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts audit-and-sensors",
        "timeout_ms": 120000
      }
    ]
  }
}
