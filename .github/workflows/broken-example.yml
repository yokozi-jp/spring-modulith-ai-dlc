name: Broken example for actionlint check
on:
  push:
    paths:
      - '.github/workflows/**'

jobs:
  broken:
    # typo: should be "ubuntu-latest"
    runs-on: ubuntu-latst
    steps:
      - uses: actions/checkout@v6
      - name: Use an undefined variable
        run: echo "${{ steps.nonexistent.outputs.value }}"
