# 環境差による文字コードの不一致とキャッシュミスを防ぐ。
org.gradle.jvmargs=-Dfile.encoding=UTF-8

# 過去のタスク出力を再利用する。
org.gradle.caching=true

# 構成済みのタスクグラフを再利用する。
org.gradle.configuration-cache=true

# 本番とローカルが適用する現在の宣言的Liquibaseスキーマタグ。
databaseSchemaTag=schema-v1
